import React from "react";

function TransferScreen(props) {
    return <div>
        <h1 className="h3 my-3">Transfer to an endpoint</h1>
        <p>Select a destination for the selected data to be transferred to.</p>
        
    </div>;
}

export default TransferScreen;